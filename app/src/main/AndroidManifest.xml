<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eu.interopehrate.dummyApplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mr2dbackup"
        android:usesCleartextTraffic="true">
        <activity android:name=".AuditInfoActivity"></activity>
        <activity
            android:name=".ConsentActivity"
            android:label="@string/title_activity_consent"
            android:theme="@style/Theme.Mr2dbackup.NoActionBar" />
        <activity
            android:name=".DownloadActivity"
            android:label="@string/title_activity_download"
            android:theme="@style/Theme.Mr2dbackup.NoActionBar" />
        <activity android:name=".LoggedInAccountActivity" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.Mr2dbackup.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>